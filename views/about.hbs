{{> head}}

    {{> navbar }} 

    <div class="row">
        <div class="col-8">
            <h1>Sobre temas de {{nombre}}</h1>
        </div>
        <div class="col">
            <img src="/assets/img/adress.png" class="img-thumbnail"/>
        </div>
    </div>

{{> footer}}